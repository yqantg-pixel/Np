local Players = game:GetService("Players")
local TweenService = game:GetService("TweenService")
local player = Players.LocalPlayer
local gui = Instance.new("ScreenGui", player:WaitForChild("PlayerGui"))
gui.Name = "GameCheckUI"
gui.IgnoreGuiInset = true
gui.ResetOnSpawn = false
local bg = Instance.new("Frame")
bg.Size = UDim2.new(1, 0, 1, 0)
bg.BackgroundColor3 = Color3.fromRGB(0, 0, 0)
bg.BackgroundTransparency = 1
bg.Parent = gui
local label = Instance.new("TextLabel")
label.Size = UDim2.new(0.8, 0, 0.15, 0)
label.Position = UDim2.new(0.1, 0, 0.4, 0)
label.BackgroundTransparency = 1
label.Text = ""
label.FontFace = Font.new("rbxasset://fonts/families/GothamSSm.json", Enum.FontWeight.SemiBold, Enum.FontStyle.Italic)
label.TextColor3 = Color3.fromRGB(255, 255, 255)
label.TextTransparency = 1
label.TextScaled = true
label.Parent = gui
local stroke = Instance.new("UIStroke", label)
stroke.Thickness = 2
stroke.Color = Color3.fromRGB(0, 200, 255)
TweenService:Create(bg, TweenInfo.new(1), {
	BackgroundTransparency = 0.4
}):Play()
local function typeText(targetText)
	TweenService:Create(label, TweenInfo.new(1.5), {TextTransparency = 0}):Play()
	for i = 1, #targetText do
		label.Text = string.sub(targetText, 1, i)
		task.wait(0.05)
	end
end
local correctId = 3808081382
task.wait(1)
if game.GameId == correctId then
	typeText("Welcome To YQANTG V3.1 ( ComeBack The Ui )")
task.delay(1.5, function()
		TweenService:Create(label, TweenInfo.new(1), {TextTransparency = 1}):Play()
		TweenService:Create(stroke, TweenInfo.new(1), {Transparency = 1}):Play()
		TweenService:Create(bg, TweenInfo.new(1), {BackgroundTransparency = 1}):Play()
	end)
	task.delay(2.2, function()
		gui:Destroy()
	end)
	task.delay(1.6, function()
local Luna = loadstring(game:HttpGet("https://raw.githubusercontent.com/YQANTGV3/YQANTGV3/refs/heads/main/source.lua.txt", true))()

local Window = Luna:CreateWindow({
	Name = "YQANTG V3.1",
	Subtitle = nil,
	LogoID = "82795327169782",
	LoadingEnabled = true,
	LoadingTitle = "YQANTG V3.1",
	LoadingSubtitle = "Welcome To YQANTG V3.1",

	ConfigSettings = {
		RootFolder = nil,
		ConfigFolder = "Big Hub"
	},

	KeySystem = false,
	KeySettings = {
		Title = "YQANTG V3.1 Key",
		Subtitle = "Key System",
		Note = "Key : BROKEN",
		SaveInRoot = false,
		SaveKey = true,
		Key = {"BROKEN"},
		SecondAction = {
			Enabled = true,
			Type = "Link",
			Parameter = ""
		}
	}
})

Window:CreateHomeTab({
	SupportedExecutors = {},
	DiscordInvite = "https://discord.gg/ys5khcTn",
	Icon = 1,
})

local Tab = Window:CreateTab({
	Name = "New",
	Icon = "announcement",
	ImageSource = "Material",
	ShowTitle = true
})

local Label = Tab:CreateLabel({
	Text = "What's New?",
	Style = 2
})
local Label = Tab:CreateLabel({
	Text = "Auto Block V11 By Notpaki",
	Style = 1
})
local Label = Tab:CreateLabel({
	Text = "Auto Block V4 By YQANTG",
	Style = 1
})
local Label = Tab:CreateLabel({
	Text = "Sit And Laydown",
	Style = 1
})
local Label = Tab:CreateLabel({
	Text = "Flip",
	Style = 1
})
local Label = Tab:CreateLabel({
	Text = "Fly",
	Style = 1
})
local Label = Tab:CreateLabel({
	Text = "Auto UpperPunch",
	Style = 1
})

local Tab = Window:CreateTab({
	Name = "Misc",
	Icon = "toc",
	ImageSource = "Material",
	ShowTitle = true
})

Tab:CreateSection("Script")

local Label = Tab:CreateLabel({
	Text = "Auto Block ↓",
	Style = 2
})

local Button = Tab:CreateButton({
	Name = "Auto Block V11",
	Description = "Auto block by Notpaki",
	Callback = function()
		loadstring(game:HttpGet("https://raw.githubusercontent.com/Cyborg883/CombatGUI/refs/heads/main/TSBCombatGUI"))()
	end
})

local Button = Tab:CreateButton({
	Name = "Auto Block V4",
	Description = "Auto block by YQANTG",
	Callback = function()
		loadstring(game:HttpGet("https://raw.githubusercontent.com/yqantg-pixel/Np/refs/heads/main/fr.txt"))()
	end
})

local Label = Tab:CreateLabel({
	Text = "Aimbot ↓",
	Style = 2
})

local Button = Tab:CreateButton({
	Name = "Slient Aim",
	Description = "Slient Aim The Body Near",
	Callback = function() 
		loadstring(game:HttpGet("https://raw.githubusercontent.com/YQANTGV2/YQANTGV2/refs/heads/main/MMM"))()
	end
})

local Button = Tab:CreateButton({
	Name = "Bot Follow + Slient Aim",
	Description = "Auto Walk And Slient Aim Near",
	Callback = function() 
		loadstring(game:HttpGet("https://raw.githubusercontent.com/YQANTGV2/YQANTGV2/refs/heads/main/Bot"))()
	end
})

local Label = Tab:CreateLabel({
	Text = "Misc ↓",
	Style = 2
})

local Button1 = Tab:CreateButton({
	Name = "Flip",
	Description = "Front Flip + Back Flip",
	Callback = function()
		loadstring(game:HttpGet("https://raw.githubusercontent.com/YQANTGV3/YQANTGV3/refs/heads/main/FL"))()
	end
})

local Button2 = Tab:CreateButton({
	Name = "Sit And Laydown",
	Description = "Sitdown + Laydown",
	Callback = function()
		loadstring(game:HttpGet("https://raw.githubusercontent.com/YQANTGV3/YQANTGV3/refs/heads/main/SL"))()
	end
})

local Button3 = Tab:CreateButton({
	Name = "Fly",
	Description = "Made By YQANTG",
	Callback = function()
		loadstring(game:HttpGet("https://raw.githubusercontent.com/YQANTGV3/YQANTGV3/refs/heads/main/FLy"))()
	end
})

local Label = Tab:CreateLabel({
	Text = "Local ↓",
	Style = 2
})

local Toggle = Tab:CreateToggle({
	Name = "Auto Respawn",
	Description = "Revive On The Spot",
	CurrentValue = false,
	Callback = function(Value)
		if Value then
			loadstring(game:HttpGet("https://raw.githubusercontent.com/YQANTGV3/YQANTGV3/refs/heads/main/RP"))()
		else
			loadstring(game:HttpGet("https://raw.githubusercontent.com/YQANTGV3/YQANTGV3/refs/heads/main/Hb"))()
		end
	end
}, "ToggleRespawnAtDeathPos")

local Toggle = Tab:CreateToggle({
	Name = "Auto Fake Out",
	Description = "When You Are Below 35 Health You Will Be Void.",
	CurrentValue = false,
	Callback = function(Value)
		if Value then
			loadstring(game:HttpGet("https://raw.githubusercontent.com/YQANTGV2/YQANTGV2/refs/heads/main/L1(FO)"))()
		else
			loadstring(game:HttpGet("https://raw.githubusercontent.com/YQANTGV2/YQANTGV2/refs/heads/main/L2(FO)"))()
		end
	end
}, "ToggleFakeOut")

local Toggle = Tab:CreateToggle({
	Name = "Invisible Block",
	Description = "When Block The Animation Will Remove",
	CurrentValue = false,
	Callback = function(Value)
		if Value then
			loadstring(game:HttpGet("https://raw.githubusercontent.com/YQANTGV3/YQANTGV3/refs/heads/main/IVB(L1)"))()
		else
			loadstring(game:HttpGet("https://raw.githubusercontent.com/YQANTGV3/YQANTGV3/refs/heads/main/IVB(L2)"))()
		end
	end
}, "InvisibleBlockToggle")

local Toggle = Tab:CreateToggle({
	Name = "No Stun",
	Description = "Prevents Being Stunned When Combo",
	CurrentValue = false,
	Callback = function(Value)
		if Value then
			loadstring(game:HttpGet("https://raw.githubusercontent.com/YQANTGV3/YQANTGV3/refs/heads/main/NST(L1)"))()
		else
			loadstring(game:HttpGet("https://raw.githubusercontent.com/YQANTGV3/YQANTGV3/refs/heads/main/NST(L2)"))()
		end
	end
}, "NoStunToggle")

local Toggle = Tab:CreateToggle({
	Name = "No Slowed",
	Description = "Avoid Stalling When Moving.",
	CurrentValue = false,
	Callback = function(Value)
		if Value then
			loadstring(game:HttpGet("https://raw.githubusercontent.com/YQANTGV3/YQANTGV3/refs/heads/main/NSL(L1)"))()
		else
			loadstring(game:HttpGet("https://raw.githubusercontent.com/YQANTGV3/YQANTGV3/refs/heads/main/NSL(L2)"))()
		end
	end
}, "NoSlowedToggle")

local delayAnimIds = {
	["rbxassetid://10480793962"] = true,
	["rbxassetid://10480796021"] = true,
	["rbxassetid://10470104242"] = true,
}

local function setupCharacter(char)
	local humanoid = char:WaitForChild("Humanoid")
	local hrp = char:WaitForChild("HumanoidRootPart")

	humanoid.AnimationPlayed:Connect(function(track)
		if getgenv().AntiDelayEnabled and delayAnimIds[track.Animation.AnimationId] then
			task.wait(0.21)
			track:Stop()
		end
	end)

	task.spawn(function()
		while char and char.Parent == workspace do
			task.wait(100)
			if hrp and hrp.PlatformStand then
				hrp.PlatformStand = true
			end
		end
	end)
end

local Players = game:GetService("Players")
local lp = Players.LocalPlayer

lp.CharacterAdded:Connect(function(char)
	task.defer(function()
		setupCharacter(char)
	end)
end)

setupCharacter(lp.Character or lp.CharacterAdded:Wait())

local Toggle = Tab:CreateToggle({
	Name = "No Slide Dash Endlag",
	Description = "Remove Delay When Dash",
	CurrentValue = false,
	Callback = function(Value)
		getgenv().AntiDelayEnabled = Value
	end
}, "Toggle")

local Toggle = Tab:CreateToggle({
	Name = "Free Rotate Ult",
	Description = "When Using Ult You Can Rotate Freely",
	CurrentValue = false,
	Callback = function(Value)
		if Value then
			loadstring(game:HttpGet("https://raw.githubusercontent.com/YQANTGV3/YQANTGV3/refs/heads/main/FRU(L1)"))()
		else
			loadstring(game:HttpGet("https://raw.githubusercontent.com/YQANTGV3/YQANTGV3/refs/heads/main/FLU(L2)"))()
		end
	end
}, "FaceCameraYToggle")

local Players = game:GetService("Players")
local Workspace = game:GetService("Workspace")

local Live = Workspace:WaitForChild("Live")
local enabled = false
local tracked = {}

local alertGui = Instance.new("ScreenGui", game.CoreGui)
alertGui.Name = "AlertNotify"

local label = Instance.new("TextLabel", alertGui)
label.Size = UDim2.new(0.4, 0, 0.08, 0)
label.Position = UDim2.new(0.5, 0, 0.03, 0)
label.AnchorPoint = Vector2.new(0.5, 0)
label.BackgroundColor3 = Color3.fromRGB(20, 20, 20)
label.BackgroundTransparency = 0.2
label.TextColor3 = Color3.fromRGB(255, 255, 255)
label.Font = Enum.Font.Gotham
label.TextSize = 20
label.Text = ""
label.Visible = false
label.BorderSizePixel = 0
label.TextStrokeTransparency = 1
label.TextWrapped = true

local function notify(text, color)
	if not enabled then return end
	label.Text = text
	label.TextColor3 = color or Color3.new(1, 1, 1)
	label.Visible = true
	task.delay(2.5, function()
		label.Visible = false
	end)
end

local function highlight(model, mode)
	if not enabled then return end
	local old = model:FindFirstChild("AlertHighlight")
	if old then old:Destroy() end

	local h = Instance.new("Highlight")
	h.Name = "AlertHighlight"
	h.FillTransparency = 0.25
	h.OutlineTransparency = 0
	h.Adornee = model

	if mode == "blue" then
		h.FillColor = Color3.fromRGB(0, 200, 255)
		h.OutlineColor = Color3.fromRGB(255, 255, 255)
	elseif mode == "red" then
		h.FillColor = Color3.fromRGB(255, 0, 0)
		h.OutlineColor = Color3.fromRGB(0, 0, 0)
	end

	h.Parent = model

	if mode == "red" then
		task.delay(11, function()
			if h and h.Parent then h:Destroy() end
		end)
	end
end

local function track(model)
	if not enabled or tracked[model] then return end
	tracked[model] = true

	local plr = Players:GetPlayerFromCharacter(model)
	local backpack = plr and plr:FindFirstChild("Backpack")

	if backpack then
		for _, t in ipairs(backpack:GetChildren()) do
			if t:IsA("Tool") and t.Name == "Death Counter" then
				highlight(model, "blue")
			end
		end

		backpack.ChildAdded:Connect(function(tool)
			if enabled and tool:IsA("Tool") and tool.Name == "Death Counter" then
				highlight(model, "blue")
				notify(plr.Name .. " Using Ultimate", Color3.fromRGB(0, 200, 255))
			end
		end)
	end

	model.ChildAdded:Connect(function(child)
		if enabled and child:IsA("Tool") and child.Name == "Death Counter" then
			task.defer(function()
				highlight(model, "red")
				local name = plr and plr.Name or model.Name
				notify(name .. " Death Counter Activated", Color3.fromRGB(255, 80, 80))
			end)
		end
	end)

	local existing = model:FindFirstChild("Death Counter")
	if existing and existing:IsA("Tool") then
		highlight(model, "red")
		local name = plr and plr.Name or model.Name
		notify(name .. " Death Counter Activated", Color3.fromRGB(255, 80, 80))
	end
end

local function setState(state)
	enabled = state
	tracked = {}

	if not enabled then
		for _, model in ipairs(Live:GetChildren()) do
			local h = model:FindFirstChild("AlertHighlight")
			if h then h:Destroy() end
		end
	else
		for _, model in ipairs(Live:GetChildren()) do
			track(model)
		end

		Live.ChildAdded:Connect(function(m)
			if enabled then
				task.delay(0.1, function()
					track(m)
				end)
			end
		end)
	end
end

local Toggle = Tab:CreateToggle({
	Name = "Death Counter Indicator",
	Description = "Hightlight Dealth Counter",
	CurrentValue = false,
	Callback = function(Value)
		setState(Value)
	end
})

local Toggle = Tab:CreateToggle({
	Name = "Ultra Shader",
	Description = "Make Better Graphics",
	CurrentValue = false,
	Flag = "UltraShaderToggle",
	Callback = function(Value)
		if Value then
			loadstring(game:HttpGet("https://raw.githubusercontent.com/YQANTGV3/YQANTGV3/refs/heads/main/US(L1)"))()
		else
			loadstring(game:HttpGet("https://raw.githubusercontent.com/YQANTGV3/YQANTGV3/refs/heads/main/US(L2)"))()
		end
	end,
})

local Label = Tab:CreateLabel({
	Text = "Other ↓",
	Style = 2
})

local Button = Tab:CreateButton({
	Name = "Orbit Gui!",
	Description = "Rotate Around Player",
    	Callback = function()
         loadstring(game:HttpGet("https://raw.githubusercontent.com/Kietba/Kietba/refs/heads/main/Orbit%20by%20YQANTG"))()
    	end
})

local Toggle = Tab:CreateToggle({
	Name = "M1 Reach",
	Description = "M1 = Tween To Player Near (Not Support Pc)",
	CurrentValue = false,
	Flag = "M1ReachToggle",
	Callback = function(Value)
		if Value then
			loadstring(game:HttpGet("https://raw.githubusercontent.com/YQANTGV3/YQANTGV3/refs/heads/main/MR(L1)"))()
		else
			_G.TweenReachHold = false
			_G.TweenReachLooping = false
		end
	end,
})

local Toggle = Tab:CreateToggle({
	Name = "Auto WallCombo Under",
	Description = "Auto WallCombo + AnyWhere : M1 X4",
	CurrentValue = false,
	Callback = function(Value)
		if Value then
			loadstring(game:HttpGet("https://raw.githubusercontent.com/YQANTGV3/YQANTGV3/refs/heads/main/ATWC(L1)"))()
		else
			_G.AutoWallCombo = false
			_G.BurrowRunning = false

			local gui = game:GetService("Players").LocalPlayer:FindFirstChild("PlayerGui")
			if gui then
				for _, v in pairs(gui:GetChildren()) do
					if v.Name == "StickGui" then
						v:Destroy()
					end
				end
			end
		end
	end
}, "Toggle")

local Players = game:GetService("Players")
local RunService = game:GetService("RunService")
local ReplicatedStorage = game:GetService("ReplicatedStorage")

local LocalPlayer = Players.LocalPlayer
local Character = LocalPlayer.Character or LocalPlayer.CharacterAdded:Wait()
local Humanoid = Character:WaitForChild("Humanoid")
local HumanoidRootPart = Character:WaitForChild("HumanoidRootPart")

local animationIDs = {
	["13294471966"] = true,
	["10469643643"] = true,
	["13295936866"] = true,
	["13378708199"] = true,
	["14136436157"] = true,
	["15162694192"] = true,
	["16552234590"] = true,
	["17889290569"] = true,
	["134775406437626"] = true,
	["10470104242"] = true,
}

local alignPos, alignOri, renderConn, targetOffsetPart
local followEnabled = false
local distanceLimit = 10
local onCooldown = false

LocalPlayer.CharacterAdded:Connect(function(char)
	Character = char
	Humanoid = char:WaitForChild("Humanoid")
	HumanoidRootPart = char:WaitForChild("HumanoidRootPart")
	setupAnimEvent()
end)

local gui = Instance.new("ScreenGui", LocalPlayer:WaitForChild("PlayerGui"))
gui.Name = "WallcomboCooldownUI"
gui.ResetOnSpawn = false

local loadFrame = Instance.new("Frame")
loadFrame.Size = UDim2.new(0, 300, 0, 20)
loadFrame.Position = UDim2.new(0.5, -150, 0.5, 40)
loadFrame.BackgroundColor3 = Color3.fromRGB(30, 30, 30)
loadFrame.Visible = false
loadFrame.Parent = gui
Instance.new("UICorner", loadFrame).CornerRadius = UDim.new(0, 12)

local bar = Instance.new("Frame", loadFrame)
bar.BackgroundColor3 = Color3.fromRGB(255, 255, 255)
bar.Size = UDim2.new(0, 0, 1, 0)
bar.Position = UDim2.new(0, 0, 0, 0)
Instance.new("UICorner", bar).CornerRadius = UDim.new(0, 12)

local label = Instance.new("TextLabel", loadFrame)
label.Size = UDim2.new(1, 0, 1, 0)
label.BackgroundTransparency = 0.7
label.TextColor3 = Color3.new(1, 0, 0)
label.Font = Enum.Font.GothamBold
label.TextScaled = true
label.Text = "Cooldown"

local function detach()
	if alignPos then alignPos:Destroy() end
	if alignOri then alignOri:Destroy() end
	if renderConn then renderConn:Disconnect() end
	if targetOffsetPart then targetOffsetPart:Destroy() end
	for _, v in ipairs(HumanoidRootPart:GetChildren()) do
		if v:IsA("Attachment") or v.Name == "HasSnapped" then v:Destroy() end
	end
end

local function attach(targetHRP)
	detach()

	targetOffsetPart = Instance.new("Part")
	targetOffsetPart.Size = Vector3.new(0.5, 0.5, 0.5)
	targetOffsetPart.Transparency = 1
	targetOffsetPart.Anchored = true
	targetOffsetPart.CanCollide = false
	targetOffsetPart.Name = "FollowPart"
	targetOffsetPart.Parent = workspace

	local att0 = Instance.new("Attachment", HumanoidRootPart)
	local att1 = Instance.new("Attachment", targetOffsetPart)

	local ori0 = Instance.new("Attachment", HumanoidRootPart)
	local ori1 = Instance.new("Attachment", targetOffsetPart)

	alignPos = Instance.new("AlignPosition")
	alignPos.Attachment0 = att0
	alignPos.Attachment1 = att1
	alignPos.RigidityEnabled = true
	alignPos.Responsiveness = 200
	alignPos.MaxForce = math.huge
	alignPos.Parent = HumanoidRootPart

	alignOri = Instance.new("AlignOrientation")
	alignOri.Attachment0 = ori0
	alignOri.Attachment1 = ori1
	alignOri.RigidityEnabled = true
	alignOri.Responsiveness = 200
	alignOri.MaxTorque = math.huge
	alignOri.Parent = HumanoidRootPart

	renderConn = RunService.RenderStepped:Connect(function()
		if targetHRP and targetHRP.Parent then
			local offset = targetHRP.CFrame * CFrame.new(0, 5, 0) * CFrame.Angles(math.rad(-90), 0, 0)
			targetOffsetPart.CFrame = offset

			if not HumanoidRootPart:FindFirstChild("HasSnapped") then
				HumanoidRootPart.CFrame = offset
				local tag = Instance.new("BoolValue")
				tag.Name = "HasSnapped"
				tag.Parent = HumanoidRootPart
			end
		end
	end)
end

local function getClosestValidTarget()
	local closest = nil
	local shortest = distanceLimit

	for _, model in pairs(workspace.Live:GetChildren()) do
		if model:IsA("Model") and model ~= Character then
			local hrp = model:FindFirstChild("HumanoidRootPart")
			local hum = model:FindFirstChildOfClass("Humanoid")
			if hrp and hum and hum.Health > 0 then
				local dist = (hrp.Position - HumanoidRootPart.Position).Magnitude
				if dist <= shortest then
					if model.Name == "Weakest Dummy" or Players:GetPlayerFromCharacter(model) then
						closest = hrp
						shortest = dist
					end
				end
			end
		end
	end

	return closest
end

local function startCooldown(duration)
	onCooldown = true
	loadFrame.Visible = true
	bar.Size = UDim2.new(0, 0, 1, 0)

	for i = 0, duration * 10 do
		local percent = i / (duration * 10)
		bar.Size = UDim2.new(percent, 0, 1, 0)
		label.Text = string.format("Cooldown: %.2f", duration - (i / 10))
		task.wait(0.1)
	end

	loadFrame.Visible = false
	onCooldown = false
end

function setupAnimEvent()
	Humanoid.AnimationPlayed:Connect(function(track)
		if not followEnabled or onCooldown then return end
		local animId = track.Animation and string.match(track.Animation.AnimationId, "%d+")
		if not animId or not animationIDs[animId] then return end

		onCooldown = true

		task.delay(0.00001, function()
			local targetHRP = getClosestValidTarget()
			if targetHRP then
				attach(targetHRP)

				task.delay(0.15, function()
					Character.Communicate:FireServer({ Goal = "LeftClick", Mobile = true })

					task.delay(0.1, function()
						for _ = 1, 2 do
							Character.Communicate:FireServer({ Goal = "Wall Combo" })
							task.wait(0.05)
						end
					end)

					task.delay(0.3, function()
						Character.Communicate:FireServer({ Goal = "LeftClickRelease", Mobile = true })
					end)
				end)

				task.delay(0.8, detach)
				task.spawn(startCooldown, 10)
			end
		end)
	end)
end

setupAnimEvent()

local Toggle = Tab:CreateToggle({
	Name = "Auto WallCombo Upper",
	Description = "Auto WallCombo + Anywhere : M1 X4 Or M1 X3 + Downslam",
	CurrentValue = false,
	Callback = function(Value)
		followEnabled = Value
	end
}, "Wallcombo")

local Toggle = Tab:CreateToggle({
	Name = "Auto Counter",
	Description = "Auto Couter Skill And M1",
	CurrentValue = false,
	Callback = function(Value)
		if Value then
			loadstring(game:HttpGet("https://raw.githubusercontent.com/YQANTGV3/YQANTGV3/refs/heads/main/ATC(L1)"))()
		else
			_G.AutoReact = false
		end
	end
}, "Toggle")

local Label = Tab:CreateLabel({
	Text = "Admin ↓",
	Style = 2
})

local Button = Tab:CreateButton({
	Name = "Infinite Yield",
	Description = nil,
	Callback = function()
		loadstring(game:HttpGet("https://raw.githubusercontent.com/EdgeIY/infiniteyield/master/source"))()
	end
})

local Button = Tab:CreateButton({
	Name = "Nameless",
	Description = nil,
	Callback = function()
		loadstring(game:HttpGet("https://github.com/FilteringEnabled/NamelessAdmin/blob/main/Source?raw=true"))()
	end
})

local Tab = Window:CreateTab({
	Name = "Farm",
	Icon = "extension",
	ImageSource = "Material",
	ShowTitle = true
})

Tab:CreateSection("Auto Farm And More")

local Toggle = Tab:CreateToggle({
	Name = "Auto Farm Near",
	Description = "Auto Farm Player Near",
	CurrentValue = false,
	Callback = function(Value)
		if Value then
			loadstring(game:HttpGet("https://raw.githubusercontent.com/YQANTGV3/YQANTGV3/refs/heads/main/ATFN"))()
		else
			_G.StickFarmOn = false

			if _G.StickFarmCleanup then
				_G.StickFarmCleanup()
				_G.StickFarmCleanup = nil
			end
		end
	end,
}, "Toggle")

local Label = Tab:CreateLabel({
	Text = "Attach ↓",
	Style = 2
})

local player = game.Players.LocalPlayer
local character = player.Character or player.CharacterAdded:Wait()
local humanoidRootPart = character:WaitForChild("HumanoidRootPart")
local camera = workspace.CurrentCamera
local RunService = game:GetService("RunService")

local screenGui = Instance.new("ScreenGui")
screenGui.Name = "StickGui"
screenGui.ResetOnSpawn = false
screenGui.Parent = player:WaitForChild("PlayerGui")

local toggleButton = Instance.new("TextButton")
toggleButton.Size = UDim2.new(0, 120, 0, 50)
toggleButton.Position = UDim2.new(0, 20, 0, 120)
toggleButton.BackgroundColor3 = Color3.fromRGB(0, 0, 0)
toggleButton.Text = "Under"
toggleButton.TextColor3 = Color3.new(1, 1, 1)
toggleButton.Font = Enum.Font.GothamBold
toggleButton.TextScaled = true
toggleButton.BorderSizePixel = 0
toggleButton.Name = "StickButton"
toggleButton.Visible = false
toggleButton.Parent = screenGui

local corner = Instance.new("UICorner")
corner.CornerRadius = UDim.new(0, 12)
corner.Parent = toggleButton
toggleButton.Active = true
toggleButton.Draggable = true

local stickOn = false
local target = nil
local connection
local noclipConnection
local oldCFrame
local oldCameraType

local function findClosestPlayer()
	local closest = nil
	local shortest = math.huge
	for _, p in pairs(game.Players:GetPlayers()) do
		if p ~= player and p.Character and p.Character:FindFirstChild("HumanoidRootPart") and p.Character:FindFirstChild("Humanoid") then
			local hrp = p.Character.HumanoidRootPart
			local dist = (hrp.Position - humanoidRootPart.Position).Magnitude
			if dist < 10000 and p.Character.Humanoid.Health > 0 and dist < shortest then
				shortest = dist
				closest = p
			end
		end
	end
	return closest
end

local function enableNoclip()
	noclipConnection = RunService.Stepped:Connect(function()
		for _, part in ipairs(character:GetDescendants()) do
			if part:IsA("BasePart") then
				part.CanCollide = false
			end
		end
	end)
end

local function disableNoclip()
	if noclipConnection then
		noclipConnection:Disconnect()
		noclipConnection = nil
	end
	for _, part in ipairs(character:GetDescendants()) do
		if part:IsA("BasePart") then
			part.CanCollide = true
		end
	end
end

local function stickToTarget()
	oldCFrame = humanoidRootPart.CFrame
	oldCameraType = camera.CameraType
	enableNoclip()
	camera.CameraType = Enum.CameraType.Scriptable

	connection = RunService.RenderStepped:Connect(function()
		if not stickOn then return end
		if not target or not target.Character or not target.Character:FindFirstChild("HumanoidRootPart") or target.Character:FindFirstChild("Humanoid").Health <= 0 then
			target = findClosestPlayer()
		end

		if target and target.Character and target.Character:FindFirstChild("HumanoidRootPart") then
			local to = target.Character.HumanoidRootPart.Position
			local from = humanoidRootPart.Position
			local look = (to - from).Unit
			local newPos = to - Vector3.new(0, 5, 0)

			local humanoid = character:FindFirstChildOfClass("Humanoid")
			if humanoid then
				humanoid.PlatformStand = true
				humanoid.Sit = false
			end
			humanoidRootPart.Velocity = Vector3.zero
			humanoidRootPart.AssemblyLinearVelocity = Vector3.zero
			humanoidRootPart.RotVelocity = Vector3.zero
			humanoidRootPart.AssemblyAngularVelocity = Vector3.zero

			humanoidRootPart.CFrame = CFrame.new(newPos, newPos + look)

			local camOffset = Vector3.new(0, 14, 20)
			local camPos = to + camOffset
			camera.CFrame = CFrame.new(camPos, to)
		end
	end)
end

local function unstick()
	if connection then
		connection:Disconnect()
		connection = nil
	end
	disableNoclip()
	local humanoid = character:FindFirstChildOfClass("Humanoid")
	if humanoid then
		humanoid.PlatformStand = false
	end
	if oldCFrame then
		humanoidRootPart.CFrame = oldCFrame
	end
	if oldCameraType then
		camera.CameraType = oldCameraType
	end
	target = nil
end

toggleButton.MouseButton1Click:Connect(function()
	stickOn = not stickOn
	toggleButton.Text = stickOn and "Under (On)" or "Under"
	if stickOn then
		stickToTarget()
	else
		unstick()
	end
end)

local Toggle = Tab:CreateToggle({
	Name = "Under",
	Description = "Attach On Under Near",
	CurrentValue = false,
	Callback = function(Value)
		toggleButton.Visible = Value
	end
})

local Players = game:GetService("Players")
local RunService = game:GetService("RunService")
local TweenService = game:GetService("TweenService")

local LocalPlayer = Players.LocalPlayer
local Character = LocalPlayer.Character or LocalPlayer.CharacterAdded:Wait()
local HumanoidRootPart = Character:WaitForChild("HumanoidRootPart")

local gui = Instance.new("ScreenGui", LocalPlayer:WaitForChild("PlayerGui"))
gui.Name = "FollowGui"
gui.ResetOnSpawn = false

local frame = Instance.new("Frame")
frame.Size = UDim2.new(0, 50, 0, 50)
frame.Position = UDim2.new(0, 10, 0.4, 0)
frame.BackgroundColor3 = Color3.fromRGB(0, 0, 0)
frame.BackgroundTransparency = 0
frame.BorderSizePixel = 0
frame.Name = "FollowFrame"
frame.Parent = gui
frame.Active = true
frame.Draggable = true
frame.Visible = false

local uicorner = Instance.new("UICorner", frame)
uicorner.CornerRadius = UDim.new(1, 0)

local button = Instance.new("TextButton")
button.Size = UDim2.new(1, 0, 1, 0)
button.BackgroundColor3 = Color3.fromRGB(60, 60, 60)
button.Text = "OFF"
button.TextColor3 = Color3.fromRGB(255, 255, 255)
button.TextScaled = true
button.Font = Enum.Font.GothamBold
button.BorderSizePixel = 0
button.Active = true
button.Draggable = true
button.Parent = frame

local btnCorner = Instance.new("UICorner", button)
btnCorner.CornerRadius = UDim.new(1, 0)

local followEnabled = false
local followConnection
local originalCFrame = nil
local lockedTarget = nil

local function getClosestTarget()
	local nearest, nearestDist = nil, 300
	for _, model in ipairs(workspace:GetDescendants()) do
		if model:IsA("Model") and model ~= Character then
			local hrp = model:FindFirstChild("HumanoidRootPart")
			local hum = model:FindFirstChildOfClass("Humanoid")
			if hrp and hum and hum.Health > 0 then
				local dist = (hrp.Position - HumanoidRootPart.Position).Magnitude
				if dist < nearestDist then
					nearest = hrp
					nearestDist = dist
				end
			end
		end
	end
	return nearest
end

local function follow()
	if followConnection then followConnection:Disconnect() end
	originalCFrame = HumanoidRootPart.CFrame
	lockedTarget = getClosestTarget()

	followConnection = RunService.RenderStepped:Connect(function()
		if lockedTarget and lockedTarget.Parent and lockedTarget:IsDescendantOf(workspace) then
			local target = lockedTarget
			local behind = target.CFrame.LookVector * -4.8
			local targetPos = target.Position + behind
			local myPos = HumanoidRootPart.Position
			HumanoidRootPart.CFrame = CFrame.new(myPos:Lerp(targetPos, 0.2), target.Position)
		end
	end)
end

local function returnToOriginal()
	if originalCFrame then
		local tween = TweenService:Create(HumanoidRootPart, TweenInfo.new(0.2, Enum.EasingStyle.Sine), {
			CFrame = originalCFrame
		})
		tween:Play()
	end
end

button.MouseButton1Click:Connect(function()
	followEnabled = not followEnabled
	button.Text = followEnabled and "ON" or "OFF"
	button.BackgroundColor3 = followEnabled and Color3.fromRGB(0, 0, 0) or Color3.fromRGB(60, 60, 60)

	if followEnabled then
		follow()
	else
		if followConnection then followConnection:Disconnect() end
		returnToOriginal()
		lockedTarget = nil
	end
end)

local Toggle = Tab:CreateToggle({
	Name = "Behind",
	Description = "Attach On Behind",
	CurrentValue = false,
	Callback = function(Value)
		frame.Visible = Value
	end
})

local Players = game:GetService("Players")
local TweenService = game:GetService("TweenService")
local RunService = game:GetService("RunService")

local LocalPlayer = Players.LocalPlayer
local Character = LocalPlayer.Character or LocalPlayer.CharacterAdded:Wait()
local HumanoidRootPart = Character:WaitForChild("HumanoidRootPart")

local gui = Instance.new("ScreenGui", LocalPlayer:WaitForChild("PlayerGui"))
gui.Name = "FollowGui"
gui.ResetOnSpawn = false

local frame = Instance.new("Frame")
frame.Size = UDim2.new(0, 50, 0, 50)
frame.Position = UDim2.new(0, 10, 0.1, 0)
frame.BackgroundColor3 = Color3.fromRGB(40, 40, 40)
frame.BorderSizePixel = 0
frame.Name = "FollowFrame"
frame.Parent = gui
frame.Active = true
frame.Draggable = true
frame.Visible = false

local uicorner = Instance.new("UICorner", frame)
uicorner.CornerRadius = UDim.new(1, 0)

local button = Instance.new("TextButton")
button.Size = UDim2.new(1, 0, 1, 0)
button.BackgroundColor3 = Color3.fromRGB(60, 60, 60)
button.Text = "OFF"
button.TextColor3 = Color3.fromRGB(255, 255, 255)
button.TextScaled = true
button.Font = Enum.Font.GothamBold
button.BorderSizePixel = 0
button.Active = true
button.Draggable = true
button.Parent = frame

local btnCorner = Instance.new("UICorner", button)
btnCorner.CornerRadius = UDim.new(1, 0)

local followEnabled = false
local alignPos = nil
local alignOri = nil
local renderConn = nil
local targetOffsetPart = nil
local originalCFrame = nil
local target = nil

local function getClosestTarget()
	local nearest, nearestDist = nil, 300
	for _, model in ipairs(workspace:GetDescendants()) do
		if model:IsA("Model") and model ~= Character then
			local hrp = model:FindFirstChild("HumanoidRootPart")
			local hum = model:FindFirstChildOfClass("Humanoid")
			if hrp and hum and hum.Health > 0 then
				local dist = (hrp.Position - HumanoidRootPart.Position).Magnitude
				if dist < nearestDist then
					nearest = hrp
					nearestDist = dist
				end
			end
		end
	end
	return nearest
end

local function attachToTarget(targetHRP)
	originalCFrame = HumanoidRootPart.CFrame

	if alignPos then alignPos:Destroy() end
	if alignOri then alignOri:Destroy() end
	if renderConn then renderConn:Disconnect() end
	if targetOffsetPart then targetOffsetPart:Destroy() end
	for _, a in HumanoidRootPart:GetChildren() do
		if a:IsA("Attachment") then a:Destroy() end
	end

	targetOffsetPart = Instance.new("Part")
	targetOffsetPart.Size = Vector3.new(0.5, 0.5, 0.5)
	targetOffsetPart.Transparency = 1
	targetOffsetPart.Anchored = true
	targetOffsetPart.CanCollide = false
	targetOffsetPart.Name = "FollowAttachPart"
	targetOffsetPart.Parent = workspace

	local att0 = Instance.new("Attachment", HumanoidRootPart)
	local att1 = Instance.new("Attachment", targetOffsetPart)
	local ori0 = Instance.new("Attachment", HumanoidRootPart)
	local ori1 = Instance.new("Attachment", targetOffsetPart)

	alignPos = Instance.new("AlignPosition")
	alignPos.Attachment0 = att0
	alignPos.Attachment1 = att1
	alignPos.RigidityEnabled = true
	alignPos.Responsiveness = 200
	alignPos.MaxForce = math.huge
	alignPos.Parent = HumanoidRootPart

	alignOri = Instance.new("AlignOrientation")
	alignOri.Attachment0 = ori0
	alignOri.Attachment1 = ori1
	alignOri.RigidityEnabled = true
	alignOri.Responsiveness = 200
	alignOri.MaxTorque = math.huge
	alignOri.Parent = HumanoidRootPart

	renderConn = RunService.RenderStepped:Connect(function()
		if not followEnabled then return end
		if targetHRP and targetHRP.Parent and targetHRP:IsDescendantOf(workspace) then
			local offsetCFrame = targetHRP.CFrame * CFrame.new(0, 5, 0) * CFrame.Angles(math.rad(-90), 0, 0)
			targetOffsetPart.CFrame = offsetCFrame
		end
	end)
end

local function detachAndReturn()
	followEnabled = false

	if alignPos then alignPos:Destroy() end
	if alignOri then alignOri:Destroy() end
	if renderConn then renderConn:Disconnect() end
	if targetOffsetPart then targetOffsetPart:Destroy() end
	for _, a in HumanoidRootPart:GetChildren() do
		if a:IsA("Attachment") then a:Destroy() end
	end

	if originalCFrame then
		local tween = TweenService:Create(HumanoidRootPart, TweenInfo.new(0.001, Enum.EasingStyle.Sine), {
			CFrame = originalCFrame
		})
		tween:Play()
	end
end

button.MouseButton1Click:Connect(function()
	followEnabled = not followEnabled
	button.Text = followEnabled and "ON" or "OFF"
	button.BackgroundColor3 = followEnabled and Color3.fromRGB(0, 170, 0) or Color3.fromRGB(60, 60, 60)

	if followEnabled then
		target = getClosestTarget()
		if target then
			attachToTarget(target)
		else
			button.Text = "NO TARGET"
			followEnabled = false
		end
	else
		detachAndReturn()
		target = nil
	end
end)

local guiToggle = Tab:CreateToggle({
	Name = "Upper",
	Description = "Attach Upper Near",
	CurrentValue = false,
	Callback = function(Value)
		frame.Visible = Value
	end
})

local Tab = Window:CreateTab({
	Name = "Shader",
	Icon = "settings_brightness",
	ImageSource = "Material",
	ShowTitle = true
})

Tab:CreateSection("Shader")
local Lighting = game:GetService("Lighting")
local function ClearShader()
	for _, v in pairs(Lighting:GetChildren()) do
		if v:IsA("PostEffect") or v:IsA("Atmosphere") then
			v:Destroy()
		end
	end
end
local ShaderPresets = {
	["Morning"] = function()
		Lighting.TimeOfDay, Lighting.ClockTime, Lighting.Brightness = "07:30:00", 7.5, 3
		Lighting.OutdoorAmbient = Color3.fromRGB(180,200,220)
		Instance.new("Atmosphere", Lighting):SetAttribute("Config", {
			Density = 0.3, Color = Color3.fromRGB(200,220,255), Decay = Color3.fromRGB(255,240,200)
		})
		local color = Instance.new("ColorCorrectionEffect", Lighting)
		color.TintColor, color.Saturation, color.Contrast, color.Brightness = Color3.fromRGB(255,255,230), 0.05, 0.15, 0.1
		Instance.new("SunRaysEffect", Lighting).Intensity = 0.25
	end,
	["Afternoon"] = function()
		Lighting.TimeOfDay, Lighting.ClockTime, Lighting.Brightness = "15:00:00", 15, 3.5
		Lighting.OutdoorAmbient = Color3.fromRGB(200,210,230)
		local atmosphere = Instance.new("Atmosphere", Lighting)
		atmosphere.Density, atmosphere.Color, atmosphere.Decay = 0.25, Color3.fromRGB(220,235,255), Color3.fromRGB(255,220,180)
		local color = Instance.new("ColorCorrectionEffect", Lighting)
		color.TintColor, color.Saturation, color.Contrast, color.Brightness = Color3.fromRGB(255,245,210), 0.1, 0.2, 0.05
		Instance.new("SunRaysEffect", Lighting).Intensity = 0.35
	end,
	["Ultra Shader Pro (HD+)"] = function()
		Lighting.Brightness, Lighting.ClockTime, Lighting.TimeOfDay = 3.2, 11, "11:00:00"
		Lighting.EnvironmentDiffuseScale, Lighting.EnvironmentSpecularScale, Lighting.GlobalShadows = 1, 1, true
		Lighting.OutdoorAmbient, Lighting.Ambient = Color3.fromRGB(75,75,75), Color3.fromRGB(105,105,105)
		Lighting.FogEnd, Lighting.FogColor = 150000, Color3.fromRGB(235,235,235)
		local atmosphere = Instance.new("Atmosphere", Lighting)
		atmosphere.Density, atmosphere.Offset, atmosphere.Glare, atmosphere.Haze = 0.1, 0.2, 0.2, 1
		atmosphere.Color, atmosphere.Decay = Color3.fromRGB(255,255,255), Color3.fromRGB(180,180,180)
		local bloom = Instance.new("BloomEffect", Lighting)
		bloom.Intensity, bloom.Threshold, bloom.Size = 0.85, 1, 38
		local dof = Instance.new("DepthOfFieldEffect", Lighting)
		dof.FocusDistance, dof.InFocusRadius, dof.NearIntensity, dof.FarIntensity = 90, 50, 0.15, 0.03
		local cc = Instance.new("ColorCorrectionEffect", Lighting)
		cc.Brightness, cc.Contrast, cc.Saturation, cc.TintColor = 0.06, 0.22, 0.15, Color3.fromRGB(255,255,255)
		local sun = Instance.new("SunRaysEffect", Lighting)
		sun.Intensity, sun.Spread = 0, 0
	local Label = Tab:CreateLabel({
	Text = "Auto UpperPunch",
	Style = 1
})
	end,
}

-- Hàm áp dụng shader
local function ApplyShader(choice)
	ClearShader()
	if ShaderPresets[choice] then
		ShaderPresets[choice]()
	end
end

local CurrentShader = "07h30 sáng"
local Dropdown = Tab:CreateDropdown({
	Name = "Chọn Shader",
	Options = {"Morning","Afternoon","Ultra Shader Pro (HD+)"},
	CurrentOption = {"07h30 sáng"},
	MultipleOptions = false,
	Callback = function(option)
		CurrentShader = option
	end
}, "ShaderDropdown")

Tab:CreateButton({
	Name = "Áp dụng Shader",
	Callback = function()
		ApplyShader(CurrentShader)
	end
})

local Tab = Window:CreateTab({
	Name = "Tech",
	Icon = "mouse",
	ImageSource = "Material",
	ShowTitle = true
})

Tab:CreateSection("Auto + Tech")

local Label = Tab:CreateLabel({
	Text = "Tech Button ↓",
	Style = 2
})

local Button = Tab:CreateButton({
	Name = "M1 Reset",
	Description = "R: M1 Reset | T: Emote Dash | H: Rotation",
	Callback = function()
		getgenv().keybinds = {
			m1reset = Enum.KeyCode.R,
			emotedash = Enum.KeyCode.T,
			rotation = Enum.KeyCode.H
		}
		loadstring(game:HttpGet("https://raw.githubusercontent.com/Slaphello/M1-Reset-And-Emote-Dash-TSB-OLD-/refs/heads/main/M1R%26ED%20TSB"))()
	end
})

local Button = Tab:CreateButton({
	Name = "Loop Dash",
	Description = "Upper Punch + Turn Off Shiftlock, Reset Or Die = Off Script",
	Callback = function()
		loadstring(game:HttpGet("https://raw.githubusercontent.com/Kietba/Kietba/refs/heads/main/Loop%20Dash%20Rework%20Script"))()
	end
})

local Button = Tab:CreateButton({
	Name = "Kai Tech",
	Description = "M1 X3 Time",
	Callback = function()
		loadstring(game:HttpGet("https://raw.githubusercontent.com/YQANTGV2/YQANTGV2/refs/heads/main/Kai"))()
	end
})

local Button = Tab:CreateButton({
	Name = "BackDash (Mobile)",
	Description = "By YQANTG",
	Callback = function()
		print("Clicked: BackDash (Mobile)")
		loadstring(game:HttpGet("https://raw.githubusercontent.com/YQANTGV3/YQANTGV3/main/backdash%20mobile"))()
	end
})

local Button = Tab:CreateButton({
	Name = "BackDash (PC)",
	Description = "By YQANTG",
	Callback = function()
		print("Clicked: BackDash (PC)")
		loadstring(game:HttpGet("https://raw.githubusercontent.com/YQANTGV3/YQANTGV3/main/backdashpc"))()
	end
})

local Button = Tab:CreateButton({
	Name = "Auto UpperPunch",
	Description = "By YQANTG",
	Callback = function()
		loadstring(game:HttpGet("https://raw.githubusercontent.com/YQANTG/YQANTG/refs/heads/main/autoupperpunch%20(1).txt"))()
	end
})


local Button = Tab:CreateButton({
	Name = "Tornado Tech",
	Description = "Upper Punch + Turn Off Shiftlock, Reset Or Die = Off Script",
	Callback = function()
		print("Clicked: Tornado Tech")
		loadstring(game:HttpGet("https://raw.githubusercontent.com/Kietba/Kietba/refs/heads/main/Tornado%20Tech%20script"))()
	end
})

local Button = Tab:CreateButton({
	Name = "Kiba Tech V1",
	Description = "M1 X3 + Upper Punch",
    	Callback = function()
         loadstring(game:HttpGet("https://raw.githubusercontent.com/YQANTG/YQANTG/refs/heads/main/kiba.txt"))()
    	end
})

local Button = Tab:CreateButton({
	Name = "Kiba Tech V2.1",
	Description = "M1 X3 + Upper Punch",
    	Callback = function()
         loadstring(game:HttpGet("https://raw.githubusercontent.com/YQANTGV3/YQANTGV3/refs/heads/main/FixTooHighh.txt"))()
    	end
})

local Button = Tab:CreateButton({
	Name = "Freeze Tech",
	Description = "",-- Creates A Description For Users to know what the button does (looks bad if you use it all the time),
    	Callback = function()
         loadstring(game:HttpGet("https://raw.githubusercontent.com/YQANTGV3/YQANTGV3/refs/heads/main/hahahajjws.txt"))()
    	end
})

local Button = Tab:CreateButton({
	Name = "Tornado Twisted",
	Description = "M1 X4",
    	Callback = function()
        loadstring(game:HttpGet("https://raw.githubusercontent.com/YQANTGV3/YQANTGV3/refs/heads/main/tornadotwisted.txt"))()
    	end
})

local Label = Tab:CreateLabel({
	Text = "Auto Tech All In One ↓",
	Style = 2
})

local Button = Tab:CreateButton({
	Name = "All In One",
	Description = "Click",
    	Callback = function()
         loadstring(game:HttpGet("https://raw.githubusercontent.com/YQANTG/YQANTG/refs/heads/main/alltechsceiot.txt"))()
    	end
})

local Tab = Window:CreateTab({
	Name = "Server",
	Icon = "language",
	ImageSource = "Material",
	ShowTitle = true
})

Tab:CreateSection("Server")

local Button_Rejoin = Tab:CreateButton({
	Name = "Rejoin",
	Description = "Auto Rejoin",
	Callback = function()
		local TeleportService = game:GetService("TeleportService")
		local player = game.Players.LocalPlayer

		local gui = Instance.new("ScreenGui")
		gui.Name = "RejoinGui"
		gui.ResetOnSpawn = false
		gui.Parent = player:WaitForChild("PlayerGui")

		local label = Instance.new("TextLabel")
		label.Size = UDim2.new(0.3, 0, 0.2, 0)
		label.Position = UDim2.new(0.35, 0, 0.4, 0)
		label.BackgroundTransparency = 1
		label.TextColor3 = Color3.new(1, 1, 1)
		label.Font = Enum.Font.GothamBold
		label.TextScaled = true
		label.Parent = gui

		for i = 0.5, 1, -1 do
			label.Text = "Rejoining in " .. i .. "..."
			task.wait(1)
		end

		gui:Destroy()
		TeleportService:Teleport(game.PlaceId, player)
	end
})

local Button = Tab:CreateButton({
	Name = "Join The Server Low Ping",
	Description = "Join The Server Low Ping",
    	Callback = function()
         
    	end
})

local Button = Tab:CreateButton({
	Name = "Server Hop",
	Description = "Next Server",
	Callback = function()
		local TeleportService = game:GetService("TeleportService")
		local HttpService = game:GetService("HttpService")
		local player = game.Players.LocalPlayer
		local placeId = game.PlaceId

		local gui = Instance.new("ScreenGui", player:WaitForChild("PlayerGui"))
		gui.Name = "ServerHopGui"
		gui.ResetOnSpawn = false

		local label = Instance.new("TextLabel", gui)
		label.Size = UDim2.new(0.3, 0, 0.2, 0)
		label.Position = UDim2.new(0.35, 0, 0.4, 0)
		label.BackgroundTransparency = 1
		label.TextColor3 = Color3.new(1, 1, 1)
		label.Font = Enum.Font.GothamBold
		label.TextScaled = true

		for i = 5, 1, -1 do
			label.Text = "Hopping in " .. i .. "..."
			task.wait(1)
		end

		gui:Destroy()

		local function getRandomServer()
			local servers = {}
			local success, result = pcall(function()
				return HttpService:JSONDecode(game:HttpGet("https://games.roblox.com/v1/games/"..placeId.."/servers/Public?sortOrder=Asc&limit=100"))
			end)

			if success and result and result.data then
				for _, s in pairs(result.data) do
					if s.playing < s.maxPlayers and s.id ~= game.JobId then
						table.insert(servers, s.id)
					end
				end
			end

			return #servers > 0 and servers[math.random(1, #servers)] or nil
		end

		local target = getRandomServer()
		if target then
			TeleportService:TeleportToPlaceInstance(placeId, target, player)
		else
			warn("⚠️ Không tìm được server khác.")
		end
	end
})

local Button = Tab:CreateButton({
	Name = "Small Server",
	Description = "Find Small Server <5 Player",
	Callback = function()
		local TeleportService = game:GetService("TeleportService")
		local HttpService = game:GetService("HttpService")
		local player = game.Players.LocalPlayer
		local placeId = game.PlaceId

		local gui = Instance.new("ScreenGui", player:WaitForChild("PlayerGui"))
		gui.Name = "SmallServerGui"
		gui.ResetOnSpawn = false

		local label = Instance.new("TextLabel", gui)
		label.Size = UDim2.new(0.3, 0, 0.2, 0)
		label.Position = UDim2.new(0.35, 0, 0.4, 0)
		label.BackgroundTransparency = 1
		label.TextColor3 = Color3.new(1, 1, 1)
		label.Font = Enum.Font.GothamBold
		label.TextScaled = true

		for i = 5, 1, -1 do
			label.Text = "Tìm server < 5 người trong " .. i .. "..."
			task.wait(1)
		end

		gui:Destroy()

		local function getSmallServer()
			local servers = {}
			local cursor = ""

			for i = 1, 5 do
				local url = "https://games.roblox.com/v1/games/"..placeId.."/servers/Public?sortOrder=Asc&limit=100"
				if cursor ~= "" then url = url .. "&cursor=" .. cursor end

				local success, data = pcall(function()
					return HttpService:JSONDecode(game:HttpGet(url))
				end)

				if success and data and data.data then
					for _, s in pairs(data.data) do
						if s.playing < 5 and s.id ~= game.JobId then
							table.insert(servers, s.id)
						end
					end

					if data.nextPageCursor then
						cursor = data.nextPageCursor
					else
						break
					end
				else
					break
				end
			end

			return #servers > 0 and servers[math.random(1, #servers)] or nil
		end

		local target = getSmallServer()
		if target then
			TeleportService:TeleportToPlaceInstance(placeId, target, player)
		else
			warn("⚠️ Không tìm được server < 5 người.")
		end
	end
})

local Button = Tab:CreateButton({
	Name = "Check Ping",
	Description = "Show Ping",
	Callback = function()
		local player = game:GetService("Players").LocalPlayer
		local screenGui = Instance.new("ScreenGui")
		screenGui.Name = "PingDisplay"
		screenGui.ResetOnSpawn = false
		screenGui.Parent = player:WaitForChild("PlayerGui")

		local pingLabel = Instance.new("TextLabel")
		pingLabel.Size = UDim2.new(0, 150, 0, 40)
		pingLabel.Position = UDim2.new(1, -160, 0, 10)
		pingLabel.BackgroundTransparency = 0.4
		pingLabel.BackgroundColor3 = Color3.fromRGB(0, 0, 0)
		pingLabel.TextColor3 = Color3.new(1, 1, 1)
		pingLabel.Font = Enum.Font.GothamBold
		pingLabel.TextSize = 20
		pingLabel.Text = "PING: -- ms"
		pingLabel.Parent = screenGui

		while true do
			local ping = math.floor(player:GetNetworkPing() * 1000)
			pingLabel.Text = "PING: " .. ping .. " ms"

			if ping <= 80 then
				pingLabel.TextColor3 = Color3.fromRGB(0, 255, 0)
			elseif ping <= 150 then
				pingLabel.TextColor3 = Color3.fromRGB(255, 170, 0)
			else
				pingLabel.TextColor3 = Color3.fromRGB(255, 0, 0)
			end

			wait(1)
		end
	end
})

local Button = Tab:CreateButton({
	Name = "Anti Lag",
	Description = "Low Graphics",
	Callback = function()
		loadstring(game:HttpGet("https://raw.githubusercontent.com/Kietba/Kietba/refs/heads/main/Antilag%20script"))()
	end
})

local Toggle = Tab:CreateToggle({
	Name = "Anti Afk",
	Description = nil,
	CurrentValue = false,
	Callback = function(Value)
		if Value then
			getgenv().AntiAFK_Connection = game:GetService("Players").LocalPlayer.Idled:Connect(function()
				local VirtualUser = game:GetService("VirtualUser")
				VirtualUser:Button2Down(Vector2.new(0, 0), workspace.CurrentCamera.CFrame)
				task.wait(1)
				VirtualUser:Button2Up(Vector2.new(0, 0), workspace.CurrentCamera.CFrame)
			end)
			warn("[✅ AntiAFK]: Enabled")
		else
			if getgenv().AntiAFK_Connection then
				getgenv().AntiAFK_Connection:Disconnect()
				getgenv().AntiAFK_Connection = nil
			end
			warn("[✅ AntiAFK]: Disabled")
		end
	end
}, "Toggle_AntiAFK")

local Tab = Window:CreateTab({
	Name = "Setting",
	Icon = "settings",
	ImageSource = "Material",
	ShowTitle = true
})

Tab:CreateSection("Setting")

local Button = Tab:CreateButton({
	Name = "Keyboard",
	Description = "Keyboard Support Mobile",
    	Callback = function()
loadstring(game:HttpGet("https://raw.githubusercontent.com/Xxtan31/Ata/main/deltakeyboardcrack.txt", true))()
    	end
})

local Players = game:GetService("Players")
local LocalPlayer = Players.LocalPlayer
local Character = LocalPlayer.Character or LocalPlayer.CharacterAdded:Wait()

local selectedColor = Color3.fromRGB(86, 171, 128)
getgenv().VFXEnabled = false

local function changeVFXColor(inst)
	if inst:IsA("ParticleEmitter") or inst:IsA("Trail") or inst:IsA("Beam") then
		inst.Color = ColorSequence.new(selectedColor)
	end
end

local function applyVFX(char)
	for _, obj in ipairs(char:GetDescendants()) do
		if getgenv().VFXEnabled then
			changeVFXColor(obj)
		end
	end

	char.DescendantAdded:Connect(function(d)
		if getgenv().VFXEnabled then
			changeVFXColor(d)
		end
	end)
end

applyVFX(Character)

LocalPlayer.CharacterAdded:Connect(function(char)
	Character = char
	if getgenv().VFXEnabled then
		applyVFX(char)
	end
end)

local Toggle = Tab:CreateToggle({
	Name = "Change Color Garou",
	Description = "Change Color By Dovi",
	CurrentValue = false,
	Callback = function(Value)
		getgenv().VFXEnabled = Value
		if Value then
			applyVFX(Character)
		end
	end
}, "Toggle")

local ColorPicker = Tab:CreateColorPicker({
	Name = "Select Garou Color",
	Color = selectedColor,
	Flag = "GarouColor",
	Callback = function(Value)
		selectedColor = Value
		if getgenv().VFXEnabled then
			applyVFX(Character)
		end
	end
}, "ColorPicker")
Tab:BuildConfigSection()
end)
else
	typeText("Game Not Support")
	task.delay(2.2, function()
		TweenService:Create(label, TweenInfo.new(1), {TextTransparency = 1}):Play()
		TweenService:Create(stroke, TweenInfo.new(1), {Transparency = 1}):Play()
		TweenService:Create(bg, TweenInfo.new(1), {BackgroundTransparency = 1}):Play()
	end)

	task.delay(3.2, function()
		player:Kick("You have been permanently banned")
	end)
end